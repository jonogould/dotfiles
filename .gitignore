vim/.netrwhist
vim/local
vim/tmp
.cache
# SpecStory explanation file
.specstory/.what-is-this.md
.env

# Local / machine-specific config — real values, never committed.
git/identity.local
git/identity-work.local
git/.gitconfig-mine
git/.gitconfig-godaddy
zsh/local.zsh
.claude/settings.local.json

# Private install profile (the .example template is tracked; real copies aren't).
dotfiles.profile
dotfiles.profile.yml
dotfiles.profile.yaml
*.profile.local

# Compiled zsh files (regenerated by zsh/compile.zsh on install)
*.zwc