# Prebuilt envsetup TUI binaries are stored in Git LFS so a fresh clone pulls
# only pointers; install.sh fetches just the one matching the host's OS/arch.
scripts/envsetup/bin/envsetup-* filter=lfs diff=lfs merge=lfs -text
